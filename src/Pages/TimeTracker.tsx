import TimeEntryForm from "../components/forms/time-entry-form/timeEntryForm";

const TimeTracker = () => {
  return (
    <div>
      <TimeEntryForm />
    </div>
  );
};

export default TimeTracker;
